<%= link_to "Discover Movies", user_dashboard_discover_index_path(@user_movies.user) %>
<% if @user_movies.type == 'top rated' %>
  <h3>Top Rated Movies</h3>
<% else %>
    <h3><%= @user_movies.type == 'search' %></h3>
<% end %>
<table>
  <tr><th>Title</th><th>Rating</th>
  <% @user_movies.movies.each do |movie| %>
    <tr>
      <td><%= movie.title %></td>
      <td><%= movie.rating %></td>
    </tr>
  <% end %>
</table>
