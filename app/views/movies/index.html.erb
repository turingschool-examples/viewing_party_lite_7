<%= link_to 'Home', '/', method: :get %>
<h1> Viewing Party </h1>
<%= button_to 'Discover Page', "/users/#{@user.id}/discover" %>
<% @facade.each do |preview| %>
  <%= link_to "#{preview.title}", "/users/#{@user.id}/movies/#{preview.id}", method: :get %>
  <%= "Vote Average: #{preview.vote}" %>
<% end %>