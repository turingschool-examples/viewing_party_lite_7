
<% if params[:top_movies].present? || params[:search].present? %>
  <% @movies.each do |movie| %>
    <li>Movie: <%= movie[:title] %>     Vote Average - <%= movie[:vote_average] %></li>
  <% end %>
<% end %>

<%= button_to 'Discover Page', discover_user_path(@user.id), method: :get %>