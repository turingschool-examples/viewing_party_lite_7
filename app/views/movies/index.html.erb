<%= button_to "Discover Page", user_discover_path(@user.id), method: :get %>

<%= render partial: "movie_results", locals: { movies: @facade.top_20_rated, user: @user, search: @facade.search } %>
<%= render partial: "movie_results", locals: { movies: @facade.keyword_results, user: @user, search: @facade.search } %>
