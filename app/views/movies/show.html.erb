<h1>Movies Show Page</h1>

<%= @movie.title %>
<%= @movie.description %>
<%= @movie.vote_average %>

<%= button_to "Create Viewing Party", new_user_movie_party_path(@user, @movie.id), method: :get %><br>
<%= button_to "Discover User Page", "/users/#{@user.id}/discover", method: :get %>