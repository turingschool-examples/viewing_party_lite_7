<%= button_to "Discover Movies", user_discover_index_path(@user.id), local: true %>

<%= button_to "Create Viewing Party", new_user_movie_viewing_party(@user.id, @movie.id), local: true %>

<%= @movie.movie_details %>