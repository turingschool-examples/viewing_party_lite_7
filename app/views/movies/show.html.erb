<%= button_to "Discover Movies", user_discover_index_path(@user), method: :get, local: true %>

<%= button_to "Create Viewing Party", new_user_movie_viewing_party_path(@user, @movie), local: true %>

<%= @movie.detailed_movie %>