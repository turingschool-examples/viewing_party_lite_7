<h1><%= @movie.title %></h1>

<%= button_to "Discover Page", user_discover_index_path(@user), method: :get, data: { turbo: false } %>

<%= button_to "Create A Party", user_movie_path(@user, @movie_id), method: :get, data: { turbo: false} %>