<!DOCTYPE html>
<html>
  <head>
    <title>Cinema Social</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "/assets/login_user.css", "/assets/new_party.css", "/assets/landing.css", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/@rails/ujs/dist/rails-ujs.min.js" %>

    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #F0F2F5;
        color: #242526;
      }

      h1 {
        font-size: 3rem;
        margin: 20px 0;
        text-align: center;
        text-transform: uppercase;
      }

      #nav_bar {
        background-color: #FFFFFF;
        padding: 20px;
        margin-bottom: 20px;
        display: flex;
        justify-content: center;
      }

      #nav_bar a {
        color: #242526;
        text-decoration: none;
        font-weight: bold;
        font-size: 1.2rem;
        margin: 0 20px;
      }

      #flash_message {
        background-color: #242526;
        color: #FFFFFF;
        padding: 10px;
        margin-bottom: 20px;
        text-align: center;
      }
    </style>
  </head>

  <body>
    <h1><span>Cinema Social</span></h1>

    <div id="nav_bar">
      <%= link_to "Home", root_path %>
    </div>

    <% flash.each do |type, msg| %>
      <div id="flash_message">
        <%= msg %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
