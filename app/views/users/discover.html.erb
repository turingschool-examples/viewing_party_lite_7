<h3> Discover Movies </h3>

<%= button_to "Top Rated Movies", user_movies_path(@user) %>

<%= form_with url: user_movies_path(@user), method: :get, local: true do |form| %>
  <%= form.label :search %>
  <%= form.text_field :search %>
  <%= form.submit "Search By Movie Title" %>
<% end %>