<%= button_to "Discover Top Rated Movies", "/users/#{@user.id}/movies", method: :get %>

<%= form_with url: "/users/#{@user.id}/movies", method: :get, local: true do |form|  %>
  <%= form.label :search_field %>
  <%= form.text_field :search_field %>
  <%= form.submit "Search" %>
<% end %>