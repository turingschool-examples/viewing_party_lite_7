<h1><%= @user.name %>'s Dashboard</h1>

<%= button_to "Discover Movies", "/users/#{@user.id}/discover"%>

<div id="viewing-parties">
  <h2>Viewing Parties</h2>
  <% @user.viewing_parties.each do |party| %>
    <ul><%= party.id %>
  <% end %>
</div>