<h1> Login Page </h1>

<%= form_with url: login_path, method: :post, data: {turbo: false}, local: true do |f|%>
  <%= f.label  :email %>
  <%= f.text_field :email %>

  <%= f.label  :password %>
  <%= f.text_field :password %>

  <%= f.submit "Login" %>
<% end %>
