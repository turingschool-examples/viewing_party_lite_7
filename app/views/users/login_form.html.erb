<h2>Log In</h2>
<div id="sign-in-form">
  <%= form_with url: login_path, method: :post do |f| %>
    <%= f.label :email %>
    <%= f.email_field :email %>
    
    <%= f.label :password %>
    <%= f.password_field :password %>
    
    <%= f.submit "Sign In" %>
  <% end %>
</div>