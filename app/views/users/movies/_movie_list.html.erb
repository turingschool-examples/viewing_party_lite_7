<% @movies.each do |movie| %>
  <div id=<%= movie.id %>>
    <div>
      <%= link_to movie.title, user_movie_path(@user, movie.id) %>
    </div>
    <div>
      <%= movie.vote_average %>
    </div>
  </div>
<% end %>