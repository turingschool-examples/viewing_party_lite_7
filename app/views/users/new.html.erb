<h2>Register New User</h2>

<%= form_with url: user_path(@user), method: :post do |f| %>
  <%= f.label :name, "Name:" %>
  <%= f.text_field :name %>

  <%= f.label :email, "Email Address:" %>
  <%= f.text_field :email %>

  <%= f.submit "Register"%>
<% end %>