<h2>Register User</h2>

<%= form_with url: "/register", method: :post, local: :true do |form|%>
  <%= form.label :name%>
  <%= form.text_field :name%>

  <%= form.label :email%>
  <%= form.text_field :email%>

  <%= form.submit "Submit"%>
<%end%>