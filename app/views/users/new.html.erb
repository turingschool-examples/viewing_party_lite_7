<h3>Register a New User</h3>

<div id = "new_user">
  <%= form_with model: @user, local: true do |f| %>
    <%= f.label :name, "Name" %>
    <%= f.text_field :name %>
    <%= f.label :email, "Email" %>
    <%= f.text_field :email %>
    <%= f.submit "Create New User" %>
  <% end %>
</div>