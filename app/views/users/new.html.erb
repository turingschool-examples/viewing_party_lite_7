<h3>New User Registration</h3>
</br>
<%= form_with(
  url: users_path,
  local: true,
  data: { turbo: false }
  )do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit "Register" %>
  <% end %>