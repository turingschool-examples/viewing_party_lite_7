<%= form_with model: @user, method: :post, local: true do |f| %>
	<%= f.label :name, "Name" %>
	<%= f.text_field :name %>
	<%= f.label :email, "Email" %>
	<%= f.email_field :email %>
	<%= f.submit "Register" %>
<% end %>