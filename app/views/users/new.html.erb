<h2>Register New User</h2>

<%= form_with model: @user do |f| %>
  <%= f.label :name, "Name:" %>
  <%= f.text_field :name %>

  <%= f.label :email, "Email Address:" %>
  <%= f.text_field :email %>

  <%= f.submit "Register"%>
<% end %>