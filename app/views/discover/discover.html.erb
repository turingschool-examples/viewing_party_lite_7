<%= form_with url: user_movies_path(@user.id), local: true do |form| %>
  <%= form.label :search, "Search For Movies" %>
  <%= form.text_field :search %>
  <%= form.submit "Search For Movie" %>
<% end %>

<p><%= button_to "Discover Top Rated Movies", user_movies_path(@user.id), method: :get %></p>
