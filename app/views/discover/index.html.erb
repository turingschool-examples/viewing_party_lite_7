<%= button_to "Top Rated Movies", user_movies_path(@user), method: :get, params: { type: :top_rated } %>

<%= form_with url: user_movies_path(@user), method: :get, local: true do |f| %>
  <%= f.label :search_movies_by_title %>
  <%= f.text_field :title %>

  <%= f.submit "Search" %>
<% end %>