<body>
<% require 'pry';binding.pry %>
  <%= button_to "Find Top Rated Movies", user_movies_path(@user), class: "button", method: :get %>

  <div>
    <%= form_tag user_movies_path(@user), method: :get do %>
    <%= text_field_tag :search %>
    <%= submit_tag "Find Movies" %>
    <% end %>
  </div>
  
</body>