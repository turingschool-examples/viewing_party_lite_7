<%= button_to "Create New User", register_path, method: :get %>
<%= link_to "Log In", login_path %>

<h3>Existing Users</h3>

<% @users.each do |user| %>
  <div id ="user_<%= user.id %>">
    <%= link_to "#{user.name} - #{user.email}", user_path(user.id) %>
  </div>
<% end %>