<h1>Viewing Party</h1>
<%= button_to "Create a New User", new_user_path %>
<div class="users">
  <ul><% @users.each do |user| %>
    <li><%= link_to "#{user.name}", user_dashboard_path(user) %></li>
  <% end %></ul>
</div>

