<h1> Viewing Party </h1>

<%= button_to "Register", "/register", {method: "get"} %>
<br>

<div id="current-users">
  <h3> Current Users </h3>
  <% @users.each do |user| %>
    <li><%= link_to "#{user.name}", "/dashboard" %></li>
  <% end %>
</div>