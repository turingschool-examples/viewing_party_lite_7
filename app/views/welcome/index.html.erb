<%= render partial: "shared/nav" %>
<h1>Viewing Party Home</h1>
<%= button_to("Create an account", '/register') %>
<% @users.each do |user| %>
<br>
    <section id="user_<%= user.id %>">
    <%= link_to "#{user.name}", user_path(user) %>
    <section>
<br>
<% end %>
