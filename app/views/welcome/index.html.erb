<section id="user-list">
  <h3>Existing Users</h3>
  <ul>
    <% @users.each do |user| %>
      <li id="user-<%= user.id%>"><%= link_to "#{user.email}", user_path(user.id) %></li>
    <% end %>
  </ul>
</section>

