
<%= button_to "Create New User", register_path, method: :get %><center>

<h3>Existing Users</h3>
<ul>
<% @users.each do |user| %>
  <li><%= link_to "#{user.name}", user_path(user) %></li>
<% end %>
</ul>