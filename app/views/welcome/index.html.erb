<h1> Viewing Party </h1>

<%= button_to "Register", "/register", {method: "get"}  %>


<ul id="current-users">
  <h3> Current Users </h3>
  <% @users.each do |user| %>
    <li><%= link_to "#{user.name}", user_path(user)%></li>
  <% end %>
</ul>