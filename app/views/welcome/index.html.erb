
<section id="new_user">
  <%= button_to "Create a New User", users_path %>
</section>
<section id="existing_users">
  <h3>Existing Users</h3>
  <ul>
    <% @users.each do |user| %>
      <li><%= user.email %></li>
    <% end %>
  </ul>
</section>