<h1><center>Viewing Party</h1>

<%= button_to "Create New User", new_user_path, method: :get %>

<h3>Users</h3>
<ul>
<% @users.each do |user| %>
  <li><%= link_to "#{user.name}", user_path(user) %></li>
<% end %>
</ul>