<div id=buttons>
  <% unless session[:user_id] %>
    <%= button_to "Log In", "/login", method: :get %>
    <br>
    <%= button_to "Create a New User", "/register", method: :get %>
    <br>
  <% else %>
    <%= button_to "Log Out", "/logout", method: :delete %>
    <br>
  <% end %>
</div>

<div id="existing-users">
  <% if session[:user_id] %>
    <h3> Existing Users </h3>
    <% @users.each do |user| %>
      <li><%= user.email %></li>
    <% end %>
  <% end %>
</div>