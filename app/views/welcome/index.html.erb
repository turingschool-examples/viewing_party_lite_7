<%= render partial: 'shared/nav' %>

<div class="container">
    <%= button_to "Create User", register_path, method: :get, class: "button" %>
</div>
<br>
<div class="container">
  <h3>Existing Users</h3>
  <% @users.each do | user | %>
    <div><%= link_to "#{user.email}", user_path(user) %></div>
  <% end %>
</div>