<h1>Viewing Party</h1>

<%= button_to 'Create New User', '/register', method: :get %>


<h2>Existing Users</h2>
<section  id="existing-users"> 
<% @users.each do |user| %>
  <ul> 
    <li id="user-<%= user.id %>"> <%= link_to user.name, "/users/#{user.id}" %> </li>
  </ul>  
<% end %>
</section>
