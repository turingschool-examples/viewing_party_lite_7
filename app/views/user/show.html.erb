<%= "#{@user.name}'s Dashboard" %>

<%= button_to "Discover Movies", user_discover_path(@user), method: :get %>

<%= "#{@user.name}'s Viewing Parties" %>
<% @viewing_parties.each do |party| %>
  <%= "Movie: #{party.movie_id}" %>
  <%= "Duration: #{party.duration}" %>
  <%= "Date: #{party.date}" %>
  <%= "Time: #{party.time}" %>
<% end %>