<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Viewing Party - Welcome</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
    }

    .welcome-container {
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      margin: 50px auto;
      padding: 20px 40px;
    }

    .welcome-container h1 {
      margin-top: 0;
      margin-bottom: 20px;
    }

    .register-button {
      background-color: #4CAF50;
      border: none;
      border-radius: 4px;
      color: white;
      cursor: pointer;
      font-size: 16px;
      padding: 10px 20px;
      text-decoration: none;
      text-transform: uppercase;
    }

    .register-button:hover {
      background-color: #45a049;
    }

    .user-list {
      list-style-type: none;
      margin: 0;
      padding: 0;
    }

    .user-list li {
      margin-bottom: 10px;
    }

    .user-list a {
      color: #333;
      text-decoration: none;
    }

    .user-list a:hover {
      color: #4CAF50;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="welcome-container">
    <h1>Welcome to Viewing Party!</h1>
    <p><%= link_to "Register New User", "/register", class: "register-button" %></p>
    <br><br>
    <ul class="user-list">
    <h3>Current users</h3>
      <% @users.each do |user| %>
        <li id="user_names<%= user.id %>">
          <%= link_to user.name, "/users/#{user.id}" %>
        </li>
      <% end %>
    </ul>
  </div>
</body>
</html>