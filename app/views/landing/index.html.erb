<%= button_to "Create New User", new_user_path, method: :get %>

<h3><%= link_to "All Users", users_path %>
<% @users.each do |user| %>
  <section id="user-<%= user.id %>">
    <%= user.name %>
  </section>
<% end %>