<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Viewing Party - Welcome</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
    }

    h1 {
      text-align: center;
      margin-top: 30px;
      margin-bottom: 30px;
    }

    .welcome-container {
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      max-width: 400px;
      margin: 0 auto;
      padding: 20px 40px;
    }

    .welcome-container h2 {
      margin-top: 0;
      margin-bottom: 20px;
    }

    .welcome-container a {
      background-color: #4CAF50;
      border: none;
      border-radius: 4px;
      color: white;
      cursor: pointer;
      display: inline-block;
      font-size: 16px;
      padding: 10px 20px;
      text-decoration: none;
      text-transform: uppercase;
      margin-bottom: 10px;
    }

    .welcome-container a:hover {
      background-color: #45a049;
    }

    .user-list {
      list-style-type: none;
      margin: 0;
      padding: 0;
    }

    .user-list li {
      margin-bottom: 10px;
    }

    .user-list a {
      color: #333;
      text-decoration: none;
    }

    .user-list a:hover {
      color: #333;
      text-decoration: none;
      background-color: transparent;
    }

  </style>
</head>
<body>
  <h1>Viewing Party</h1>
  <div class="welcome-container">
    <h2>Welcome</h2>
    <% if session[:user_id] %>
      <%= link_to "Log Out", logout_path, method: :delete %>
      
      <%= link_to "My Discoveries", "/users/#{session[:user_id]}" %>
      <p>Registered Users:</p>
    <ul class="user-list">
      <% @users.each do |user| %>
        <li id="user_names<%= user.id %>">
          <p><%= user.email%></p>
        </li>
      <% end %>
    </ul>
    <% else %>
      <%= link_to 'Create a New User', register_path %>
      <br>
      <%= link_to "Log In", login_path %>
    <% end %>
    
  </div>
</body>
</html>