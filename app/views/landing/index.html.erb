<h1> Viewing Party</h1>

<%= button_to "Create New User", "/users", method: :post %>
<br>
<br>

<h3> Existing Users</h3>

<% @users.each do |user| %>
  <li><%= link_to user.email, user_path(user)%></li>
<% end %>