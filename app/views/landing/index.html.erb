<h1>Landing Page</h1>

<h3>Existing Users</h3>
<ul>
<% @users.each do |user| %>
<div id="user-<%= user.id %>">
  <b><li><%= link_to "#{user.name}", "/dashboard/#{user.id}" %></li></b>
</div>
<% end %>
</ul>