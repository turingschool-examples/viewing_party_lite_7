<h1>Viewing Party Lite<h1>
<p><%= button_to "Create New User", "/register", method: :get %></p>
<div id="existing_users">
  <ul><% @users.each do |user| %>
    <li><%= link_to "#{user.name}", "/users/#{user.id}" %></li>
  <% end %></ul>
</div>