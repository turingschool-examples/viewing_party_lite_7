<h1> Viewing Party </h1>

<%= button_to "Create New User", "/register" %>

<h3> Existing Users </h3>
<% @users.each do |user| %>
  <%= link_to user.email, "/users/#{user.id}" %>
  <br>
  <br>
<% end %>